---
layout: default
meta_title: Blog
meta_description: Blog page description
body_class: blog
title: The Solarflux Blog
---

<main>
  <section class="max-w-6xl px-4 mx-auto my-8 lg:my-16">
    <h1 class="text-5xl">Insights</h1>
  </section>

  <section class="max-w-5xl px-4 mx-auto mt-16 mb-16 lg:mt-24" aria-label="Blogs">

    <div class="posts">

      {% for post in collections.blog %}

      <article class="grid lg:grid-cols-2 lg:gap-6">

        <a class="relative block overflow-hidden featured-image-link" href="{{ post.url }}">

          <figure>
            <img data-lazy="{{ post.data.featured_image }}" class="block">
          </figure>

        </a>

        <div class="py-6">

          <div class="{{ post.data.category | categoryStyle}}">
            <a href="/blog/{{ post.data.category | lower }}/" class="px-2 py-1 text-sm no-underline rounded-full">{{ post.data.category }}</a>
          </div>

          <h2 class="mt-3 mb-2 text-xl">
            <a class="block text-2xl font-semibold text-gray-800 hover:text-gray-600" href="{{ post.url }}">{{ post.data.title }}</a>
          </h2>

          <div class="mb-8 text-base text-gray-600 md:flex">
            <svg
                class="w-6 h-6 mr-2"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                />
            </svg>
            <time datetime="{{ post.date }}">
              {{ post.date | dateDisplay }}
            </time>

          </div>

          <p class="text-lg leading-normal text-gray-800">{{ post.data.excerpt }}</p>

          <div class="flex items-center mt-8">
            <a href="{{ post.url }}" class="mr-1 leading-none text-primaryPurple hover:underline hover:text-primaryPurpleDark">Continue Reading</a>
            <svg class="w-4 h-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 5l7 7-7 7M5 5l7 7-7 7" />
            </svg>
          </div>

        </div>

      </article>

      {% endfor %}

    </div>

  </section>

</main>
